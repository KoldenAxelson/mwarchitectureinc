# Astro front-end — copy to .env and set the contact endpoint URL.
# This is the public Worker URL emitted after `wrangler deploy`.
# Use http://localhost:8787 for local dev with `wrangler dev`.
PUBLIC_CONTACT_ENDPOINT=https://mw-contact.<your-subdomain>.workers.dev
